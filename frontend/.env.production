# Vercel environment variables
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Backend URL (from Render deployment)
VITE_API_URL=https://exam-portal-backend.onrender.com

# WebSocket URL (same as backend, use wss://)
VITE_WS_URL=wss://exam-portal-backend.onrender.com/ws

# API timeout for slow cold starts
VITE_API_TIMEOUT=30000
